# 开发计划-生物谷资讯全天抓取与每小时任务
创建时间：2026-03-30
当前状态：已完成
执行目标：把生物谷资讯抓取默认改成按当天全天过滤，并把容器内调度默认改成每小时执行一次。
步骤：[x] 明确新增需求  [x] 补失败测试  [x] 修改当天过滤逻辑  [x] 调整调度默认值  [x] 更新文档与回填结果
改动明细：
- 修改 `deploy/docker/bioon_news.py`：新增 `target_date` 参数、默认 `limit=100`、按目标日期过滤首页文章、将 `latest/news_latest.json` 改为同一天增量合并。
- 修改 `deploy/docker/bioon_news_scheduler.py`：默认启用定时触发，默认周期改为 `3600` 秒。
- 修改 `tests/docker/test_bioon_news.py`：新增“按目标日期过滤”“调度默认值”“同一天 latest 累积合并”测试。
- 更新 `操作方法记录/01-04` 文档，使默认行为与当前实现保持一致。
校验结果：
- 执行 `python -m pytest tests/docker/test_bioon_news.py -q`，结果：`7 passed in 0.31s`。
- 执行 `python -m py_compile deploy/docker/bioon_news.py deploy/docker/bioon_news_scheduler.py deploy/docker/server.py tests/docker/test_bioon_news.py`，结果：通过。
- 执行正式抓取验证：`target_date=2026-03-30`、`force_refresh=true`、`limit=100`，结果：`processed_count=12`、`new_count=12`、`failed_count=0`，并成功写入 `runtime/bioon_news/latest/news_latest.json`。
