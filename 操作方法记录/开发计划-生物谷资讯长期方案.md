# 开发计划-生物谷资讯长期方案
创建时间：2026-03-30
当前状态：已完成
执行目标：在当前 Docker 服务中实现生物谷最新资讯长期抓取能力，支持手动 API 触发与容器内定时触发，并输出完整操作文档。
步骤：[x] 方案确认  [x] 写入方案与操作文档  [x] 编写失败测试  [x] 实现统一抓取内核与 API  [x] 接入定时触发  [x] 校验并回填结果
改动明细：
- 新增 `deploy/docker/bioon_news.py`，实现首页解析、详情解析、结果落盘、状态查询与业务路由。
- 新增 `deploy/docker/bioon_news_scheduler.py`，实现容器内低频定时触发器。
- 修改 `deploy/docker/server.py`，挂载 `/custom/bioon-news/*` 接口。
- 修改 `deploy/docker/supervisord.conf`，托管调度进程。
- 新增 `tests/docker/test_bioon_news.py`，覆盖首页解析、详情解析、路由暴露和结果落盘。
- 新增 `操作方法记录/` 下方案、Docker、手动触发、定时任务、结果文件与排障文档。
校验结果：
- 使用 `.venv` 执行 `python -m pytest tests/docker/test_bioon_news.py -q`，结果：`4 passed in 0.27s`。
- 执行 `python -m py_compile deploy/docker/bioon_news.py deploy/docker/server.py deploy/docker/bioon_news_scheduler.py tests/docker/test_bioon_news.py`，结果：通过。
- 执行真实站点抓取验证，`limit=1` 时返回 `success=true`、`new_count=1`、`failed_count=0`，并成功写出 `latest/news_latest.json`。
