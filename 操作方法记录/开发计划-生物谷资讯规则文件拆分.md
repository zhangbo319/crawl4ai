# 开发计划-生物谷资讯规则文件拆分
创建时间：2026-03-30
当前状态：已完成
执行目标：把生物谷资讯抓取规则拆分为独立规则文件，并补齐规则位置、修改方式、生效方式的说明文档。
步骤：[x] 明确新增需求  [x] 拆分规则文件  [x] 运行测试与语法校验  [x] 更新操作文档  [x] 重启容器并验证  [x] 回填结果
改动明细：
- 新增 `deploy/docker/bioon_news_rules.json`，集中管理首页、详情页选择器与默认字段值。
- 修改 `deploy/docker/bioon_news.py`，支持从规则文件加载选择器、默认值与请求配置。
- 修改 `deploy/docker/bioon_news_scheduler.py`，把定时任务默认抓取上限从 `10` 提升到 `100`，避免每小时任务漏抓当天首页文章。
- 修改 `tests/docker/test_bioon_news.py`，新增外部规则文件生效测试，并校验定时任务默认 `limit=100`。
- 更新 `操作方法记录/00`、`01`、`02`、`03`、`05`，补充规则文件位置、修改方式、重启容器后的生效方式。
校验结果：
- 执行 `/Users/zhangbo/work/INKE/study/crawl4ai/.venv/bin/python -m pytest tests/docker/test_bioon_news.py -q`，结果：`9 passed in 0.31s`。
- 执行 `/Users/zhangbo/work/INKE/study/crawl4ai/.venv/bin/python -m py_compile deploy/docker/bioon_news.py deploy/docker/bioon_news_scheduler.py deploy/docker/server.py tests/docker/test_bioon_news.py`，结果：通过。
- 重启容器 `crawl4ai-bioon` 后执行 `GET /health`，结果：`{"status":"ok","version":"0.8.6"}`。
- 执行 `GET /custom/bioon-news/status`，结果：`latest_file=/app/runtime/bioon_news/latest/news_latest.json`，`total_seen_urls=16`。
- 执行 `POST /custom/bioon-news/run`，参数：`limit=100,target_date=2026-03-30,force_refresh=true`，结果：`processed_count=15`、`new_count=15`、`failed_count=0`。
